import { NumberToRotatePipe } from './number-to-rotate.pipe';

describe('NumberToRotatePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToRotatePipe();
    expect(pipe).toBeTruthy();
  });
});
